name: Commando Shell

on: [push]

jobs:
  commando:
    runs-on: ubuntu-22.04
    steps:
      - name: echo something
        run: |
          cat $GITHUB_ENV
          cat $GITHUB_OUTPUT
          echo "${{ job.status }}"